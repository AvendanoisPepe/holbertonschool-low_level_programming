#include "main.h"
/**
*/
void more_numbers(void)
{
	int contador1 = 0;
	int contador2 = 0;

	while (contador1 < 10)
	{
		while (contador2 < 15)
		{
		        if (contador2 >= 10)
				_putchar(contador2 / 10 + '0');
			_putchar(contador2 % 10 + '0');
			contador2++;
		}
		_putchar('\n');
		contador1++;
	}
}
