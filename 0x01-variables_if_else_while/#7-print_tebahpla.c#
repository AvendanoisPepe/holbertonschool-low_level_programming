#include <stdio.h>
/**
*Prints the alphabet upside down 
*Return:0 if there is no error
*/
int main(void)
{
  char ch;

  for (ch = 'z'; ch >= 'a'; ch--)
    {
      putchar(ch);
    }
  putchar('\n');
return (0);
}
