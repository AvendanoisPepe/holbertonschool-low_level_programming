#include <stdio.h>

/**
*main-the function prints :with proper grammar, but the outcome is a piece of art,
*Return:0 if there is no error
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);
}
